# Plan: plan-2026-08-15T21-36-02
> Generated: 2026-08-15T21:36:02.012Z
> Tier: 2
> Drift: dirty working tree; dead cross-references in docs

## Security Invariants
All actions must respect:
- NEVER: force-push (git push --force)
- NEVER: edit LFS binaries (.bin.gz, .gguf, .safetensors) directly
- NEVER: edit src-rust/ without reading TSD spec first
- NEVER: bypass checkNetworkConsent() before network calls
- NEVER: bypass validateEndpointUrl() for SSRF protection
- NEVER: store PII (emails, real names, addresses)
- NEVER: hardcode db.version(N+1) — always use this.version(this.nextVersionAfter(N))
- NEVER: edit .bin.gz SHAs by hand — run node scripts/ocrArtifacts.mjs unlock
- NEVER: use console.log — use logger.ts
- NEVER: import React in engine files
- NEVER: use tesseract.js — use runOCR from src/lib/ocrEngine.ts
- NEVER: use new URL(template literal) with import.meta.url — use fetch() with absolute paths
- ALWAYS: call epistemicShadow.interrupt() on user prompts
- ALWAYS: read TSD spec before modifying Rust/WASM or core engine files
- ALWAYS: use EA_NITI_OCR_STRICT=1 for release-grade runs
- ALWAYS: run all 8 gates in order before any PR
- ALWAYS: prefer runtime verification over trusting doc footers

## Drift Items
- [ ] dirty working tree
- [ ] dead cross-references in docs

## Proposed Actions
- Clean untracked junk files (logs, .DS_Store, stale scratch)
- Remove or fix dead file references in markdown docs

## Status
- [ ] Auto-approved (Tier 1/2)
- [x] Applied
- [x] Gates passed
- [ ] PR created